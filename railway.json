{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --no-cache && echo build-$(date +%s)"
  },
  "deploy": {
    "startCommand": "node server.js",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE"
  }
}
